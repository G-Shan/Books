#To run a file my-program.py that contains python code from the command prompt, use::
$ python my-program.py

# Search a python package in command prompt
sudo pip search numpy

# Download a python package in cmd
sudo pip install numpy
sudo apt-get install numpy

#Alternatively we can install it in rodeo interface
import pip
pip.main(['install', 'numpy'])


